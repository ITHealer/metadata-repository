# commerce_demo

## Description

Deterministic ClickHouse schema for the metadata review demo

## Tables

| Name | Columns | Comment | Type |
| ---- | ------- | ------- | ---- |
| [customers](customers.md) | 5 | Customer dimension at one row per customer; contains synthetic PII-like fields. | MergeTree |
| [order_items](order_items.md) | 5 | Order detail fact at one row per order_id and line_number. | MergeTree |
| [orders](orders.md) | 6 | Order fact at one row per order_id; cancelled orders remain in the table. | MergeTree |

## Relations

```mermaid
erDiagram

"order_items" }o--|| "orders" : "order_items.order_id -> orders.order_id"
"orders" }o--|| "customers" : "orders.customer_id -> customers.customer_id"

"customers" {
  DateTime created_at "UTC timestamp when the customer profile was created"
  UUID customer_id "Stable identifier for one demo customer"
  String email "Synthetic contact email classified as PII; always uses the .test domain"
  String full_name "Display name; synthetic data used only by this demo"
  LowCardinality_String_ segment "Business segment: retail, premium, or enterprise"
}
"order_items" {
  UInt16 line_number "One-based line position within an order"
  UUID order_id "Parent order; logical join to orders.order_id"
  String product_code "Synthetic product code captured when the order was placed"
  UInt16 quantity "Number of product units on this order line"
  Decimal_18__2_ unit_price "Price per unit in VND when the order was placed"
}
"orders" {
  DateTime created_at "UTC timestamp when the order was created"
  UUID customer_id "Customer that placed the order; logical join to customers.customer_id"
  UUID order_id "Stable identifier for one order"
  LowCardinality_String_ order_status "Current lifecycle state: pending, paid, shipped, or cancelled"
  Decimal_18__2_ total_amount "Order total in VND after discounts"
  DateTime updated_at "UTC timestamp of the latest order update"
}
```

---

> Generated by [tbls](https://github.com/k1LoW/tbls)
